{
  "name": "json-server-tournament",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch mock/db.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}